#include "main.h"

/**
 * _abs - returning absoulte value of number
 *
 * @n: number to get absolute
 *
 * Return: absolute value of n
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	return (n);
}
